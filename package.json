{
  "name": "portfolio-root",
  "private": true,
  "workspaces": [
    "portfolio-web",
    "portfolio-cms"
  ],
  "scripts": {
    "web:dev": "npm run dev --workspace=portfolio-web",
    "web:build": "npm run build --workspace=portfolio-web",
    "cms:dev": "npm run dev --workspace=portfolio-cms",
    "cms:build": "npm run build --workspace=portfolio-cms"
  }
}
